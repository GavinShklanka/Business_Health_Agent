


Startup misc: Important Clutter

Install uv (if not installed)
pip install uv

Create virtual environment
uv venv


Activate (PowerShell):

.venv\Scripts\activate

uv pip install langchain langgraph openai selenium webdriver-manager beautifulsoup4 requests fastapi uvicorn python-dotenv

uv pip install langchain-openai

setx OPENAI_API_KEY "your_key_here"
